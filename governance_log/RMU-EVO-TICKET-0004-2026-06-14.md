# RECONSTRUCTION_MAP_UPDATE — Chat de Lila / P6.5 File Reader

## Componente
Server/GUI — `cgalpha_v3/gui/server.py` (P6.5 del Nexus)

## Fase / Ticket
EVO-TICKET-0004 — P6.5 File Reader (G4 building block)

## 1. Cambios realizados
| Archivo | Cambio | Razón |
|---|---|---|
| `cgalpha_v3/gui/server.py` | Endpoint `/api/admin/read-file` añadido | Lectura dinámica de archivos del proyecto desde el chat de Lila |

**Archivos NO tocados:** `deferred_outcome_monitor.py`, `oracle.py`, endpoints existentes.

**Nota de desviación de especificación (registrada):**
- Ruta en `P65_FILE_READER_SPEC.md`: `/api/lila/read-file`
- Ruta implementada: `/api/admin/read-file`
- Funcionalidad: idéntica. La desviación no se registró en la sesión de implementación. No requiere corrección inmediata, pero si la ruta se expone en documentación pública o el Codex, debe reflejar la ruta real.

**Verificación:** `py_compile` (Cat.2 simple, sin suite de tests dedicada al endpoint).

**Invariantes del endpoint implementados:**
1. `path` siempre resuelto contra `_PROJECT_ROOT_READER` (no path traversal)
2. Solo GET — nunca modifica archivos
3. `@require_auth` obligatorio
4. Archivos >100KB sin rango → 413

## 2. Tests
No hay pytest dedicado. Verificado con `py_compile` (sintaxis). Los 4 casos de `P65_FILE_READER_SPEC.md` no tienen test automatizado — deuda menor si el endpoint evoluciona.

## 3. Decisiones arquitectónicas
- [ ] Sin ADR — Cat.2 sin alternativas arquitectónicas en disputa. Fue implementación directa contra spec.

**Nota:** la elección de `/api/admin/` vs `/api/lila/` como prefijo no se documentó. Retroactivamente: `/api/admin/` sugiere un endpoint de operador, que es coherente con el caso de uso (el operador lee archivos del proyecto via chat). No requiere ADR.

## 4. LLM Readability Check (D-010)

1. ¿Un modelo menos capaz puede continuar desde aquí sin reconstruir el contexto completo de esta sesión?
   **SÍ** — el endpoint existe en `server.py`, los invariantes están en `P65_FILE_READER_SPEC.md`, y la desviación de ruta está documentada en el cuerpo del ticket y en este RMU. Un modelo futuro que extienda el File Reader puede encontrar todo lo necesario sin reconstruir la sesión.

2. ¿Los archivos modificados son auto-explicativos sin este RMU?
   **PARCIALMENTE** — el endpoint en `server.py` es legible, pero sin este RMU la desviación de ruta (`/api/admin/` vs `/api/lila/`) parecería arbitraria, y P65_FILE_READER_SPEC.md seguiría documentando una ruta que no es la real.

3. ¿Hay decisiones implícitas que deberían convertirse en ADR antes de cerrar?
   **NO** — la única ambigüedad (prefijo de ruta) es menor y no tiene alternativas arquitectónicas. Se documenta aquí como nota, no como ADR.

## 5. Estado post-cambio
- Ticket: EVO-TICKET-0004
- Maturity alcanzada: MATURITY_5
- Estado en ciclo: IMPLEMENTED (2026-06-14)
- Debt class: EXPANSION_DEBT
- Deuda técnica pendiente:
  - `P65_FILE_READER_SPEC.md` documenta la ruta `/api/lila/read-file` — desincronizado con la implementación real. Corregir si se actualiza la spec.
  - Tests automatizados para los 4 casos del spec no existen.
  - Los gaps G2/G3/G5 del chat de Lila (P6.5) siguen pendientes — prerequisito para ellos era este ticket.

## 6. Próximo paso recomendado
Generar RMU de EVO-TICKET-0003 (heartbeat watchdog). Con G4 desbloqueado por este ticket, la siguiente sesión sobre P6.5 puede atacar G2 (Capa0+Capa1+Harness en el endpoint de chat).
