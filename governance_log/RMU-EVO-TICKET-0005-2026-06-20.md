# RECONSTRUCTION_MAP_UPDATE — TripleCoincidenceDetector

## Componente
TripleCoincidenceDetector — `cgalpha_v3/infrastructure/signal_detector/triple_coincidence.py`
LiveDataFeedAdapter — `cgalpha_v3/application/live_adapter.py` (warm_start)

## Fase / Ticket
EVO-TICKET-0005 — TripleCoincidenceDetector: zone cleanup + warm_start fix

## 1. Cambios realizados
| Archivo | Cambio | Razón |
|---|---|---|
| `cgalpha_v3/infrastructure/signal_detector/triple_coincidence.py` | `_cleanup_expired_zones()`: criterio de expiración cambiado de `candle_index` (no estable entre reinicios) a timestamp+distancia ATR | Bug: zonas obsoletas reaparecían tras restart (Punto Ciego #4) |
| `cgalpha_v3/infrastructure/signal_detector/triple_coincidence.py` | `warm_start()`: ahora reproduce detección histórica sobre el buffer, no solo lo hidrata | Bug: zonas persistidas nunca se renovaban tras reinicio |
| `cgalpha_v3/infrastructure/signal_detector/triple_coincidence.py` | `feed_kline_for_zone_detection()`: precálculo de tendencias ZigZag sobre buffer completo en lugar de por-vela | Bug: con 60 velas rara vez encontraba segmento válido |
| `cgalpha_v3/infrastructure/signal_detector/triple_coincidence.py` | `zone_max_distance_atr=5.0` marcado `PROVISIONAL` en comentario | Calibration pending: valor basado en intuición, no en percentiles reales |

**Commits:**
- `e984c61` — fix stale active zones after restart (2026-06-19)
- `2155bdd` — correct debt class + add calibration_pending flag (2026-06-19)
- `6b64972` — confounders verified: same detector config in test vs production (2026-06-20)

**Verificación de despliegue (Ruta C, paso 7):**
- PID 309946 (inicio 2026-06-19 20:12:51) era ANTERIOR al commit del fix (20:21:27) — fix nunca se ejecutó durante 10h de diagnóstico. Falso positivo de "fix aplicado".
- PID 460712 (inicio 2026-06-20 06:37:54) POSTERIOR al commit ✅
- Criterio #1: zonas viejas (66.5k-66.0k) no reaparecieron tras restart real ✅
- Criterio #2/#3: no verificables durante sesión — mercado en régimen atípicamente quieto (~$600 de rango). Separado como deuda de calibración en EVO-TICKET-0006.

**Lección de proceso incorporada:** "Commiteado ≠ Desplegado". Ruta C Paso 7 (verificar timestamp de inicio de proceso > timestamp de commit) añadido a `LILA_ROUTING_PROMPT.md` y validado en su primer uso real (EVO-TICKET-0006).

## 2. Tests
Tests preexistentes: 2 fallan / 2 pasan — idénticos antes y después del fix.
Verificado con `git checkout <commit>^ -- archivos` + pytest: mismo resultado = no regresión.
`pytest --collect-only` confirmó 4 tests recolectados sin errores de import silenciosos.

Test aislado de control (misma instanciación que producción, verificado commit 6b64972):
- 200 velas 1m → 0 zonas | 500 velas 5m → 9 zonas
- Barrido de velas heartbeat (10%/50% volume=0) → resultado idéntico. Confusor descartado.

## 3. Decisiones arquitectónicas
- [x] Nueva decisión en ADR: `aipha_memory/identity/ADR-EVO-TICKET-0005-1-cleanup-por-tiempo-distancia.md`
- Resumen: cleanup por timestamp+distancia ATR vs índice de buffer. Alternativa (índice estable cross-restart) descartada porque requiere persistir el índice absoluto de vela, que introduce su propia deuda de sincronización. Ver ADR para detalle completo.

**Nota sobre la deuda de calibración** (`calibration_pending: true`):
- `zone_max_distance_atr=5.0` — valor provisional, no calibrado con percentiles reales.
- Método requerido: percentile analysis sobre ≥200 ciclos de detección reales.
- No bloquea este ticket — la calibración se separa como parte de EVO-TICKET-0006 (resolución: Opción A, volver a 5m, que es donde hay datos de calibración disponibles).

## 4. LLM Readability Check (D-010)

1. ¿Un modelo menos capaz puede continuar desde aquí sin reconstruir el contexto completo de esta sesión?
   **SÍ** — el cuerpo del ticket en EVO_TICKET_LOG.md es excepcionalmente detallado: 4 rondas de Ruta A, arqueología de git, hipótesis descartadas, causa raíz exacta (timestamp de proceso vs commit), y las verificaciones que la sostienen. El ADR de 0005 cubre la decisión de cleanup. No hay información crítica fuera del log.

2. ¿Los archivos modificados son auto-explicativos sin este RMU?
   **PARCIALMENTE** — el comentario `PROVISIONAL` en `zone_max_distance_atr` marca la deuda, pero no explica la alternativa elegida para el cleanup. El ADR-0005 complementa esto. Este RMU añade la lección de "Commiteado ≠ Desplegado" que no está en el código.

3. ¿Hay decisiones implícitas que deberían convertirse en ADR antes de cerrar?
   **NO** — la única decisión arquitectónica (cleanup por tiempo+distancia vs índice estable) está en el ADR-0005. La decisión de timeframe (1m vs 5m) ya tiene ADR-0006. La calibración de `zone_max_distance_atr` no es una decisión arquitectónica sino un parámetro operacional pendiente de datos.

## 5. Estado post-cambio
- Ticket: EVO-TICKET-0005
- Maturity alcanzada: MATURITY_4 (fix verificado; no llega a MATURITY_5 por calibration_pending en zone_max_distance_atr)
- Estado en ciclo: EXECUTING → cierre formal pendiente de calibración o decisión explícita de diferir
- Debt class: CONSOLIDATION_DEBT + `calibration_pending: true` (flag ortogonal, no clase de deuda)
- Deuda técnica pendiente:
  - `zone_max_distance_atr=5.0` marcado PROVISIONAL — requiere percentile analysis ≥200 ciclos de detección
  - CRB de P5 (TripleCoincidenceDetector) — este ticket es la primera evidencia real de por qué P5 necesita CRB propio
  - PC#7 (single clock source dependency) añadido como nuevo Punto Ciego — pendiente actualizar `architectural_analysis.md`

## 6. Próximo paso recomendado
Generar RMU de EVO-TICKET-0004 (File Reader). Iniciar CRB de P5 cuando haya capacidad, no antes de completar la sesión de RMUs en bloque.
